import { ReactElement } from "react";

export interface ConsumptionAttributes {
    icon: ReactElement;
    type: string;
    unit: string;
    color: string;
    label: string;
    colorMuted: string;
}
