import { PlaceholderCard } from "@/components/app/common/placeholderCard";

const RecommendationPage = () => {
  return <PlaceholderCard>Select a Recommendation</PlaceholderCard>;
};

export default RecommendationPage;
